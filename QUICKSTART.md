# Быстрый старт

Минимальная инструкция для запуска парсера за 5 минут.

## Шаг 1: Установка зависимостей (1 минута)

```bash
pip install -r requirements.txt
```

## Шаг 2: Настройка Google Sheets API (3 минуты)

### 2.1 Получение credentials.json

1. Откройте [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте проект
3. Включите "Google Sheets API"
4. Создайте Service Account
5. Создайте JSON ключ → скачайте как `credentials.json`
6. Поместите в корень проекта

### 2.2 Создание таблицы

1. Создайте [новую Google Sheets таблицу](https://sheets.google.com/)
2. Назовите: `Blogger Stats`
3. Откройте `credentials.json` → скопируйте `client_email`
4. В таблице: "Поделиться" → вставьте email → права "Редактор"

**Детальная инструкция:** [SETUP.md](SETUP.md)

## Шаг 3: Настройка блогеров (1 минута)

Отредактируйте `config.json`:

```json
{
  "spreadsheet_name": "Blogger Stats",
  "bloggers": [
    {
      "name": "Ваш Блогер",
      "youtube": "https://www.youtube.com/@channel",
      "tiktok": "https://www.tiktok.com/@username",
      "instagram": "https://www.instagram.com/username"
    }
  ]
}
```

## Шаг 4: Запуск

```bash
python main.py
```

## Проверка работы

```bash
# Опциональный тест
python test_parsers.py
```

## Структура проекта

```
parcing/
├── main.py                 # ← Запускаете это
├── config.json             # ← Настраиваете блогеров здесь
├── credentials.json        # ← Создайте этот файл
├── processed_videos.json   # Автоматически создается
├── requirements.txt
├── parsers/                # Парсеры платформ
├── utils/                  # Вспомогательные модули
└── logs/                   # Логи работы
```

## Быстрые команды

```bash
# Установка
pip install -r requirements.txt

# Запуск
python main.py

# Тест
python test_parsers.py

# Просмотр логов (Windows)
type logs\parser_20260123.log

# Просмотр логов (Linux/Mac)
tail -f logs/parser_20260123.log
```

## Что дальше?

После первого успешного запуска:

1. **Автоматизация** - настройте ежедневный запуск
   - [AUTOMATION.md](AUTOMATION.md)

2. **Расширенная настройка** - добавьте больше блогеров
   - Редактируйте `config.json`

3. **Анализ данных** - просматривайте статистику в Google Sheets
   - Создавайте графики и дашборды

## Частые проблемы

### "credentials.json not found"
→ Создайте файл согласно Шагу 2

### "SpreadsheetNotFound"
→ Проверьте название в `config.json` и доступ Service Account

### Парсер не находит видео
→ Проверьте URL в `config.json`, убедитесь что профили публичные

**Полный FAQ:** [FAQ.md](FAQ.md)

## Документация

- 📖 [README.md](README.md) - полная документация
- 🚀 [SETUP.md](SETUP.md) - детальная установка
- ⚙️ [AUTOMATION.md](AUTOMATION.md) - автоматизация
- ❓ [FAQ.md](FAQ.md) - вопросы и ответы

## Структура Google Sheets таблицы

После запуска вы увидите:

| Блогер | Платформа | Дата публикации | Дата парсинга | Название ролика | URL | Просмотры | Лайки | Комментарии | Репосты | Время просмотра |
|--------|-----------|-----------------|---------------|-----------------|-----|-----------|-------|-------------|---------|-----------------|

---

**Готово!** Запустите `python main.py` и проверьте результаты в Google Sheets.
